/*
Null
In TypeScript, `null` is a special type that represents 
an intentional absence of value. It is one of JavaScript's 
primitive values and behaves differently depending on 
TypeScript's strictness settings.
*/

let emptyValue: null = null;
// emptyValue = 20; // Type '20' is not assignable to type 'null'.
