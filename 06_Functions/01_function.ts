/* 
Function
A block of reusable code that performs a 
specific task and can have typed parameters 
and return values in TypeScript.
*/

function greet(name: string): string {
  return `Hello, ${name}!`;
}

console.log(greet("Ashish")); // Hello, Ashish!
