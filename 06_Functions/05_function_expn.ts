/*
Function Expressions
Functions assigned to variables, allowing them to be stored, 
passed as arguments, or used as callbacks.
*/

const add = function (a: number, b: number): number {
  return a + b;
};

console.log(add(5, 10)); // 15
